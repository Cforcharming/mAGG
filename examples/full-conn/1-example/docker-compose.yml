networks:
  backend:
    driver: bridge
  frontend:
    driver: bridge
services:
  py1:
    image: python
    networks:
    - frontend
  tc:
    image: tomcat
    networks:
    - frontend
    ports:
    - '80'
version: '3.8'
