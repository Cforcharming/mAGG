version: "3.8"

services:

  py:
    networks:
      - backend
      - db
    image: python

  tc:
    networks:
      - frontend
      - backend
    image: tomcat
    ports:
      - "3001:8080"

  ms:
    # build: .
    networks:
      - db
    image: mysql

  ng:
    networks:
      - frontend
      - backend
    image: nginx

  at:
    networks:
      - backend
    image: atsea_app

  atd:
    networks:
      - backend
    image: atsea_db

networks:
  frontend: {}
  backend: {}
  db: {}
